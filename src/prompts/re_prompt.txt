你是一位顶级的知识图谱关系抽取专家，特别擅长材料科学和冶金领域。

你的任务是从下面提供的【文本块】中，抽取出【实体列表】中各个实体之间存在的关系。

【文本块】:
{{CHUNK_TEXT}}

【实体列表】:
这是上述文本块中出现的、已经过消歧的规范化实体列表。请只在这些实体之间寻找关系。
{{ENTITIES_IN_CHUNK}}

【预定义关系类型】:
请只使用以下预定义的关系类型。
{{RELATION_TYPES}}

【示例】
正面示例：如果文本是“高炉利用焦炭将铁矿石还原”，你应该抽取出 `[["高炉", "使用原料", "焦炭"]]`。
反面示例：你不应该抽取出 `[["焦炭", "使用设备", "高炉"]]`，因为焦炭不是设备。

请严格遵循以下规则：
1.  抽取的关系必须是 `[头实体, 关系类型, 尾实体]` 的三元组格式。
2.  **[最重要规则]** 三元组的头实体（head）和尾实体（tail）**必须，且只能**从【实体列表】中**一字不差地**选择。严禁生成任何不在列表中的新实体。
3.  关系类型必须是【预定义关系类型】中的一种。
4.  你的最终输出必须是一个包含这些三元组的JSON列表 `[]`。如果文本中无法建立任何符合要求的关系，请返回一个空列表。
5.  不要添加任何解释或无关的文字，直接返回JSON列表。

例如，如果文本块是“高炉是炼铁系统的核心设备”，实体列表是["高炉", "炼铁系统"]，关系类型是["是...的核心设备"]，你应该抽取出 `[["高炉", "是...的核心设备", "炼铁系统"]]`。