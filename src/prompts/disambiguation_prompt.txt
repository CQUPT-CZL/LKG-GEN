你是一位顶级的知识图谱和材料科学专家。

你的任务是分析下面提供的【实体列表】，并将所有语义上相同或指向同一个概念的实体进行聚类。

【实体列表】:
{{ENTITY_LIST_JSON}}

请严格遵循以下规则进行处理：
1.  **聚类同义词**: 将所有意思相同的实体分为一组。例如，"炼铁系统" 和 "iron-making system" 应该在同一组。
2.  **确定规范名称**: 对于每一个聚类出的组，请从组内所有实体中选择一个最常用、最完整的 `entity_text` 作为该组的 "canonical_name" (规范名称)。
3.  **处理复合实体**: 对于用逗号分隔的实体（如 "煤、焦炭"），请将其视为独立的实体来考虑。

你的输出必须是一个严格的JSON对象，格式如下。除了JSON，不要返回任何其他解释性文字。

【输出格式】:
{
  "clusters": [
    {
      "canonical_name": "选出的规范名称1",
      "aliases": ["组内所有实体的entity_text列表"]
    },
    {
      "canonical_name": "选出的规范名称2",
      "aliases": ["组内所有实体的entity_text列表"]
    }
  ]
}